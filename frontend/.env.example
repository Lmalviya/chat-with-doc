# Base URL for the FastAPI backend — no trailing slash
VITE_BACKEND_API_BASE_URL=http://localhost:8000

# Set to "true" to show LangSmith "view trace" links on AI messages (dev/admin builds only)
VITE_DEV_TRACE=false
